# Extra plugins (exporters etc.) prepared for GenIce3.
# Loaded by Utilities/replacer.py for docs and templates.

plugins:
  exporter:
    - name: genice3-svg
      package: genice3-svg
    - name: genice3-py3dmol
      package: genice3-py3dmol
    - name: genice3-mdanalysis
      package: genice3-mdanalysis
